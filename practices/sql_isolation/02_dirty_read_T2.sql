-- ============================================================
-- АНОМАЛИЯ: DIRTY READ (Грязное чтение)
-- ТРАНЗАКЦИЯ T2 — выполняется в Терминале 2
-- ============================================================
--
-- ПОРЯДОК ВЫПОЛНЕНИЯ:
--   [T1-1] Сначала выполните шаг [T1-1] в Терминале 1
--   [T2-1] Затем выполните блок "[T2-1]" здесь
--   [T2-2] Выполните блок "[T2-2]" — попытка dirty read
--   [T1-2] Переключитесь на Терминал 1, выполните [T1-2]
--   [T2-3] Вернитесь, выполните блок "[T2-3]"
-- ============================================================

-- ===================== [T2-1] =====================
BEGIN;
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;

-- ===================== [T2-2] =====================
-- Попытка прочитать незакоммиченные данные T1

SELECT id, name, balance FROM accounts WHERE name = 'Alice';

-- ===================== [T2-3] =====================
-- После того как T1 сделала ROLLBACK

SELECT id, name, balance FROM accounts WHERE name = 'Alice';

COMMIT;
