-- ============================================================
-- АНОМАЛИЯ: NON-REPEATABLE READ (Неповторяющееся чтение)
-- ТРАНЗАКЦИЯ T2 — выполняется в Терминале 2
-- ============================================================
--
-- ПОРЯДОК ВЫПОЛНЕНИЯ:
--   Сначала выполните [T1-1] в Терминале 1
--   Затем выполните [T2-1] здесь
--   Переключитесь на Терминал 1, выполните [T1-2]
-- ============================================================

-- ===================== [T2-1] =====================
-- Выполняется ПОСЛЕ того как T1 сделала первое чтение

BEGIN;

UPDATE accounts SET balance = 1500.00 WHERE name = 'Alice';

COMMIT;

SELECT id, name, balance FROM accounts WHERE name = 'Alice';
