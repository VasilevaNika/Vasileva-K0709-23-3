-- ============================================================
-- АНОМАЛИЯ: PHANTOM READ (Фантомное чтение)
-- ТРАНЗАКЦИЯ T2 — выполняется в Терминале 2
-- ============================================================
--
-- ПОРЯДОК ВЫПОЛНЕНИЯ:
--   Сначала выполните [T1-1] в Терминале 1
--   Затем выполните [T2-1] здесь
--   Переключитесь на Терминал 1, выполните [T1-2]
-- ============================================================

-- ===================== [T2-1] =====================
-- Выполняется ПОСЛЕ того как T1 сделала первый COUNT

BEGIN;

INSERT INTO products (name, price, category)
VALUES ('Tablet', 25000.00, 'Electronics');

COMMIT;

SELECT id, name, price, category
FROM products
WHERE category = 'Electronics'
ORDER BY id;
